<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencoding.wherego.repository.interfaces.AccomodationRepository">
	
	<select id="findById" resultType="com.tencoding.wherego.dto.AccomodationDto">
		select A.acc_no,  A.acc_host_no, A.acc_name, A.acc_address,  
			   A.acc_type, A.acc_info, A.acc_options, A.acc_price,
		       A.acc_delete, A.review_count, B.img_url 
		from accomodation AS A 
		left join acc_img AS B
		on A.acc_no = B.acc_no and acc_delete = 0
		where A.acc_no = #{accNo}
	</select>
	 
</mapper>